import { SecurityManagementView } from '../../sections/security-management/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Ville e residenze di Lusso | EPA',
  description:
    'Soluzioni di security management su misura per ville, residenze e dimore esclusive, conformi agli standard internazionali, per garantire serentità e privacy.',
};

export default function SecurityManagementPage() {
  return <SecurityManagementView />;
}
